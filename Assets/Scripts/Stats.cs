public class Stats
{
    public float MaxHealth, Health, Armor, MovementSpeed, Power;

    public Stats(float maxHealth, float health, float armor, float movementSpeed, float power)
    {
        MaxHealth = maxHealth;
        Health = health;
        Armor = armor;
        MovementSpeed = movementSpeed;
        Power = power;
    }
}
